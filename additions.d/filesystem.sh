# always create intermediate directories if necessary
alias mkdir='mkdir -p'

# display the (up to) 10 most recent files
alias nf='ls -lAt | head'

# display the (up to) 10 oldest files
alias of='ls -lAt | tail'
